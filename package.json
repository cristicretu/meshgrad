{
  "name": "meshgrad-root",
  "private": true,
  "scripts": {
    "build": "pnpm -F meshgrad build",
    "dev": "pnpm -F meshgrad build --watch",
    "website": "pnpm -F meshgrad-website dev",
    "preinstall": "npx only-allow pnpm"
  },
  "devDependencies": {
    "tsup": "6.2.1",
    "typescript": "4.6.4"
  },
  "packageManager": "pnpm@7.9.0"
}
