declare const generateJSXMeshGradient: (length: number, baseColor?: string, hash?: number, customColors?: string[]) => {
    backgroundColor: string;
    backgroundImage: string;
};
declare const generateMeshGradient: (length: number, baseColor?: string, hash?: number, customColors?: string[]) => {
    backgroundColor: string;
    backgroundImage: string;
};

export { generateJSXMeshGradient, generateMeshGradient };
